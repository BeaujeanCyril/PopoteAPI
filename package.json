{
  "name": "popote-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API backend for enriched batch cooking recipes",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "nuxt start",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "dotenv": "^17.2.3",
    "nuxt": "3.17.7",
    "prisma": "^6.19.1"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.3"
  }
}
